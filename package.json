{
  "name": "robot-trading-kline-dashboard",
  "version": "1.0.0",
  "description": "Filesystem-backed robot trading dashboard using K-line data, indicators, websocket updates, and paper auto-trading.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/dayTradeBacktrace.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED"
}
